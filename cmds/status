#!/bin/bash

__run__() {
    gb -is-in-repo || return 1

    echo "Current Branch: $(gb -get-current-branch)"
    echo "# Untracked"
    gb -get-untracked | sed 's/^/   /'
    echo "# Modified"
    gb -get-modified | sed 's/^/   /'
    echo "# Added"
    gb -get-staged | sed 's/^/   /'
}
